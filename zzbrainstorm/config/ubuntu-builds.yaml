# Batch builds (YAML)
# Each entry overrides defaults in config/defaults.yaml
# Both `version` and `release` are REQUIRED - copy from catalog file.
# VMID is auto-generated as: {node_digit}{distro_digit}{version}

builds:
  # Ubuntu 24.04 LTS
  - distro: ubuntu
    version: "24.04"
    release: noble
    notes: "Ubuntu 24.04 LTS (Noble Numbat)"

  # Ubuntu 22.04 LTS
  - distro: ubuntu
    version: "22.04"
    release: jammy
    notes: "Ubuntu 22.04 LTS (Jammy Jellyfish)"

  # Ubuntu 20.04 LTS  
  - distro: ubuntu
    version: "20.04"
    release: focal
    notes: "Ubuntu 20.04 LTS (Focal Fossa)"
    override:
      cloud_init:
        user: sysadmin
        password_file: /root/.ci_pass
        search_domain: lab.example.com
        ssh_keys_id: jdoe
      vm:
        cores: 2
        memory: 2048
        disk_size: 20G
      tags:
        - cloud-init
        - template
        - ubuntu
        - "20.04"
      cache:
        dir: ./cache
        keep: true
      behavior:
        non_interactive: false
        no_syslog: false

