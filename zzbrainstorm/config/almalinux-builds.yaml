# Batch builds (YAML)
# Each entry overrides defaults in config/defaults.yaml

builds:
  # Minimal required fields example
  - vmid: 509000
    storage: SSD-03A
    distro: almalinux
    release: "9"
    version: "9"
    notes: "AlmaLinux 9 (GenericCloud)"

  - vmid: 508000
    storage: SSD-03A
    distro: almalinux
    release: "8"
    version: "8"
    notes: "AlmaLinux 8 (GenericCloud)"
